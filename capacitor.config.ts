import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'WorkoutApplicationMobileApp',
  webDir: 'www'
};

export default config;
